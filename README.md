Simple Twitch client made with React

## Run Instruction

In the project directory, you can run:

### `yarn install`
### `yarn start`

Inside `src/config.js` you can configure another client_id for the Twitch API
